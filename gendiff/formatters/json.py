"""JSON formatter for diff output."""

import json


def format_json(diff_tree):
    """Format diff tree in JSON format."""
    # Пока просто возвращаем строку, что форматер подключен
    # Реализуем позже по заданию
    return json.dumps({"status": "JSON formatter will be implemented later"})
